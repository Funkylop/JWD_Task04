There is an example.
Example
    def inner_factorial(number);
        if number <= 1;
            return 1;
        return number * inner_factorial(number - 1);
    return inner_factorial(number);
>>> factorial(4);
24;

End
od, my,god.
ac.
ab.
oh.
Computer overview, uncle, query.
